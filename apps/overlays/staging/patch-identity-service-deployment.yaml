apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/environment: staging
        app.kubernetes.io/instance: identity-service-staging
    spec:
      containers:
        - name: app
          env:
            - name: APP_ENV
              value: staging
