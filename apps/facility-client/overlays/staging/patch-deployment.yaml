apiVersion: apps/v1
kind: Deployment
metadata:
  name: facility-client
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/environment: staging
    spec:
      containers:
        - name: app
          env:
            - name: APP_ENV
              value: staging
